# ── 运行时生成的数据 ────────────────────────────────
# SQLite 数据库、WAL 日志、旧 JSON 存档，首次启动自动重建
data/
dist/data/
*.db
*.db-wal
*.db-shm
narrator.json
narrator.json.migrated

# ── 含密钥的本机配置 ────────────────────────────────
# 本机 config.json 里有真实 API key，绝不入库。
# 模板见 config.example.json，克隆后复制一份再填。
config.json
!dist/config.json

# ── 打包产物 ────────────────────────────────────────
# 可由 dist/ 重新压缩生成，不入库
dist.zip
*.zip
*.tar.gz

# ── 依赖 ────────────────────────────────────────────
node_modules/
package-lock.json
yarn.lock

# ── 系统与编辑器 ────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.log

# ── WorkBuddy 工作笔记 ──────────────────────────────
# 智能体的项目记忆与日志，属于工具私有数据，不属于项目源码。
# 如果想把它一并纳入版本管理，删掉下面这行即可。
.workbuddy/
