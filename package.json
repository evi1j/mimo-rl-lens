{
  "name": "mimo-train-live",
  "version": "1.4.0",
  "private": true,
  "description": "训练实况看板：指标曲线 + 指标讲解（固定文案 + AI 讲解）",
  "scripts": {
    "start": "node server.js",
    "setup": "node scripts/setup.js",
    "build": "node scripts/sync-dist.js",
    "build:check": "node scripts/sync-dist.js --check",
    "build:zip": "node scripts/sync-dist.js --zip",
    "config:example": "node scripts/gen-config-example.js",
    "config:check": "node scripts/gen-config-example.js --check",
    "test": "node scripts/run-tests.js"
  },
  "_note": "build* 只管生成 dist/（打包分发时才跑，提交代码不触发）；config:* 由提交钩子自动跑，也可手动补模板",
  "optionalDependencies": {
    "node-sqlite3-wasm": "^0.8.60"
  },
  "_deps_note": "正常情况用不到它：Node ≥22.5 自带 node:sqlite。只有 Node 太旧时才由它兜底（纯 wasm，无需编译），所以放在 optionalDependencies —— 装不上也不影响安装流程"
}
