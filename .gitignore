# ── 运行时生成的数据 ────────────────────────────────
# SQLite 数据库、WAL 日志，首次启动自动重建（存档只走 SQLite，不再有 JSON 存档）
data/
*.db
*.db-wal
*.db-shm

# ── 含密钥的本机配置 ────────────────────────────────
# 本机 config.json 里有真实 API key，绝不入库。
# 模板见 config.example.json，克隆后复制一份再填。
config.json

# ── 构建产物 ────────────────────────────────────────
# dist/ 是打包分发用的目录，由 `node scripts/sync-dist.js` 从源码生成，
# 只在要打包时才构建 —— 不入库，免得源码提交和部署产物提交混在一起。
# 分发包里的 README、start.command、start.bat 源文件在 deploy/。
dist/

# 压缩包也是构建产物，由 `node scripts/sync-dist.js --zip` 生成
dist.zip
*.zip
*.tar.gz

# ── 依赖 ────────────────────────────────────────────
node_modules/
yarn.lock

# package-lock.json 入库（不在这里忽略）：本项目是应用，不是给别人当依赖的库。
# 提交 lockfile 才能保证别人 npm install 装到的 node-sqlite3-wasm 版本，
# 跟本地验证过的完全一致。只有发布给别人的库才该忽略 lock。

# ── 系统与编辑器 ────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.log

# ── WorkBuddy 工作笔记 ──────────────────────────────
# 智能体的项目记忆与日志，属于工具私有数据，不属于项目源码。
# 如果想把它一并纳入版本管理，删掉下面这行即可。
.workbuddy/
