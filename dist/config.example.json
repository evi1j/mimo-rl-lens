{
  "_comment": "配置模板。真实配置是 config.json（已被 .gitignore 排除，因为里面会写 API key）。用法：cp config.example.json config.json，然后按需填写。文件缺失时服务也能启动，会自动用内置默认值 + 规则引擎解说。",
  "llm": {
    "enabled": false,
    "baseUrl": "http://127.0.0.1:8090/v1",
    "apiKey": "",
    "model": "",
    "temperature": 0.3,
    "timeoutMs": 180000,
    "maxTokens": 4000,
    "reasoningEffort": "",
    "maxItemsPerPoll": 3,

    "_retry": "讲解链路的分层重试。讲解分两段：工具轮查数据、正文轮写讲解；任一段都可能被网络抖动、上游 5xx、思考吃满额度（正文空）、流中途断连打断。这里分别重试，不整条重来，已查到的数据不会白查一遍",
    "retryMax": 2,
    "bodyAttempts": 3,
    "enoughChars": 200,
    "retryBackoffMs": 800
  }
}
