# Docker 构建上下文排除表。
# 必须放在仓库根目录 —— Docker 只认 <构建上下文>/.dockerignore，
# 放 docker/ 里不起作用（构建上下文是仓库根，因为镜像要 src/ 和 public/）。
#
# 两个目的：
#   1) 别把本机密钥（config.json 里有真实 API key）打进镜像层；
#   2) 别把 node_modules（上千个文件）当上下文传来传去，构建会慢一大截。

# ── 含密钥的本机配置 ──────────────────────────────
config.json

# ── 运行时生成的数据 ──────────────────────────────
# 首次启动自动重建，不该进镜像（进了也会被 volume 覆盖）
data/
*.db
*.db-wal
*.db-shm

# ── 依赖与构建产物 ────────────────────────────────
node_modules/
dist/
dist.zip
*.zip
*.tar.gz

# ── 运行时用不到的仓库内容 ────────────────────────
.git/
.githooks/
.workbuddy/
deploy/
docker/
scripts/
test/
*.md

# ── 系统与编辑器 ──────────────────────────────────
.DS_Store
*.log
*.swp
